source big-grammar/big.grammar.foma

read lexc big-grammar/big.sr.lexicon.lexc
define SRLexicon;

define CorrectMadePredictions [SRLexicon & Grammar];
define CorrectUnmadePredictions [SRLexicon - Grammar];
define IncorrectPredictions [Grammar - SRLexicon];

regex CorrectUnmadePredictions;
pairs > correct

regex IncorrectPredictions;
pairs > incorrect

regex CorrectMadePredictions;
pairs > correctly-derived
